apiVersion: v1
kind: Service
metadata:
  name: vprodb
spec:
  ports:
  - port: {{ .Values.db.servicePort }}
    targetPort: vprodb-port
    protocol: TCP
  selector:
    app: vprodb
  type: ClusterIP
