apiVersion: v1
kind: Service
metadata:
  name: car-ser-service
  namespace: car-ser
spec:
  type: ClusterIP
  ports:
    - name: tcp
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: car-ser-deployment
  sessionAffinity: None
